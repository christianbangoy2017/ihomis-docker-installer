# Production backups (never commit)
hospital_dbo_*.sql
backup_*.sql

# Allow ONLY the seed archive
!db/hospital_dbo_seed.zip

# SSL keys and certs
ssl/*.key
ssl/*.crt

# Docker volumes
db/data/

# Environment secrets
.env
*.env

# OS junk
.DS_Store
Thumbs.db
